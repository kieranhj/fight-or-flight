node_modules
dist
dist-ssr
*.local
.DS_Store

# Vite / build
.vite
*.tsbuildinfo

# Wrangler / Cloudflare
.wrangler
.dev.vars
# Filled-in anonymous deploy config (real domain + ids) must never be committed;
# only the .example template is tracked. See docs/ANONYMOUS-HOSTING.md.
worker/wrangler.anon.toml

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
